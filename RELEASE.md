## 0.3.0

* **Fixes:**
  * Corrected the generated POM file for Gradle artifacts to ensure proper dependency resolution (`eecfe49`).
* **Improvements:**
  * Now plugin supports gradle caching properly
  * Updated to support Kotlin `2.2.20`.
  * Improved automatic architecture detection (`os.arch`) for more reliable builds across different systems.
  * General refactoring and enhancements
  * Added support for scijava loader
